# P1-1 联调反馈（前端 → 后端）

> **历史流程已由 R2-00A 取代（2026-08-26）**：本文保留当时联调证据，不得将其中 `create_all()` / `init_db()` 后执行 `alembic stamp head` 的做法用于当前数据库。请遵循 [`docs/06-启动说明.md`](../../06-启动说明.md) 与当前后端迁移说明，先分类/备份旧库，再通过 Alembic 迁移。

> 本地 `integration/p1-1` 合并 `origin/backend/phase-p1-1`（`8831a2b`）与 `origin/frontend/p1-1`（`74c76c1`）后，八项 Mock 均为 `false` 的联调结论。

---

## 前端交付摘要

| 项 | 说明 |
| --- | --- |
| 集成分支 | `integration/p1-1` @ `9f46589`（merge backend + frontend） |
| 后端 | `origin/backend/phase-p1-1` @ `8831a2b` |
| 前端 | `origin/frontend/p1-1` @ `74c76c1` + 联调适配 commit |
| 契约 | [`api-contract-p1-1.md`](api-contract/api-contract-p1-1.md) |
| Mock | AUTH/BASIC_DATA/SCHEDULE/NODE_DISPATCH/ROUTES/SIMULATION/EXCEPTIONS/AI 均为 `false` |
| 构建 | `npm run build` 通过 |

### 联调适配（前端）

| 文件 | 改动 |
| --- | --- |
| `utils/schedule-normalize.ts` | `goods_schedules.path` 对象数组 → `path[]` + `path_labels[]` |
| `utils/dispatch-normalize.ts` | `package_details` → `package_codes` |
| `api/schedule.ts` | 真实 API 响应走 normalize |
| `ScheduleSummaryCards.vue` | `score_display` 展示「越高越好」，tooltip 含原始分 |

---

## 环境

| 项 | 值 |
| --- | --- |
| 数据库 | 删 `src/backend/data/logistics.db` → `Base.metadata.create_all()` → `init_demo_data.py` |
| 后端 | `uvicorn main:app --port 8000` |
| 前端 | `npm run dev`，`.env.local` 8 项 Mock 全 `false` |
| 账号 | `dispatcher` / `123456` |
| 自测脚本 | [`test_p1_1_integration.py`](../src/backend/scripts/test_p1_1_integration.py) |

---

## 联调结论（2026-06-25 第 1 轮）

**总体：通过**（P1-1 验收项 1～4 API 对齐；前端 breaking change 已适配；无 P0 阻塞）

### P1-1 验收（§4.1 项 1～4）

| # | 检查项 | 结果 | 说明 |
| --- | --- | --- | --- |
| 1 | 全局方案评分可读 | pass | `GET /schedule/global` 返回 `score_display`；新建方案首条可能为 `0`（raw=max 时归一化公式所致，见 P1 偏差） |
| 2 | 货物路径节点名 + 详情 | pass | `goods_schedules.path` 为 `{node_code, node_name}[]`；前端 normalize 后搜索/展示正常；`GET /goods/{code}`、`/orders/{code}` 200 |
| 3 | 节点调度搜索 + 详情 | pass | `GET /schedule/batches/{code}` tasks 含 `from_node_name`、`package_details`；前端 derive `package_codes` |
| 4 | 编号可点详情 | pass | 货物/订单 detail API 200（UI 依赖 Dashboard 集中 drawer） |

### API 冒烟摘录

```
login ok
created GS20260625001 score_display 0
path object ok SC003 …(node_name)
packages count 77
batch BATCH20260625001
from_node_name …
package_details 1
goods detail ok GO001_1
order detail ok O001
ALL_P1_1_API_CHECKS_PASS
```

### MVP 主链路

| 链路 | 结果 |
| --- | --- |
| 真实登录 | pass |
| 生成全局调度 | pass |
| 节点间调度 demo_mode=true | pass |
| `npm run build` | pass |
| 路径规划 / 模拟送达 | 待 UI 手测（API 层 node-dispatch 已通过） |

---

## P1 偏差（非阻塞）

| 级别 | 项 | 说明 |
| --- | --- | --- |
| P1 | 首条方案 `score_display=0` | 当 `raw_score == max_possible` 时归一化为 0；第二条方案后应恢复正常；可考虑后端首条默认 100 或延迟 max 计算 |
| P1 | `packages[]` 无 `*_node_name` | 契约仅 code；包裹一览联调时显示纯 code（已接受） |
| P1 | manager 调 `/ai/parse` | 延续阶段 8 已知偏差 |

---

## 后续动作

1. 前端联调修复已 commit 到 `frontend/p1-1`
2. 后端 `backend/phase-p1-1` 与前端各自 PR 合 `main`
3. 合并前 smoke：`integration/p1-1` 上再跑一轮 UI 手测（Dashboard 四项 + 路径 + 模拟送达）
4. 联调完成后 `.env.local` 可恢复 Mock `true` 便于独立开发

---

## 第 1 轮联调结论

**P1-1 前后端联调通过**，可进入 PR 合并 `main` 流程。
