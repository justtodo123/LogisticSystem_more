# 阶段 8 联调反馈（前端 → 后端）

> 本地 `integration/phase-8` 合并 `origin/backend/phase-8`（`d49b3ce`）与 `origin/frontend/phase-8`（`81c9d75`）后，八项 Mock 均为 `false`、火山引擎 ARK DeepSeek 已配置的联调结论。

---

## 前端交付摘要

| 项 | 说明 |
|----|------|
| 分支 | `frontend/phase-8` @ `81c9d75` |
| 页面 | Dashboard 内嵌 `AiAssistantPanel`（dispatcher 可见；无方案时仍展示，支持 AI 新建） |
| API | [`ai.ts`](../src/frontend/src/api/ai.ts)（`parseAi` 60s timeout + `postWithMeta` 保留降级信息） |
| Mock | `VITE_USE_MOCK_AI`；联调时 **8 项均为 `false`** |
| 契约 | 对齐 [`api-contract-phase8.md`](api-contract/api-contract-phase8.md) |
| 构建 | `npm run build` 通过（~1.1s） |

---

## 功能清单

| 能力 | 说明 |
|------|------|
| 自然语言 | textarea + 发送并执行 / 仅预览参数（dry-run） |
| 执行目标 | 新建调度 / 重规划当前方案 / 多选历史方案 |
| 手动权重 | F007/F005 权重滑块 + F006 max_iterations |
| 降级展示 | `meta.degraded=true` → `el-alert` warning，不伪装 AI 成功 |
| 成功刷新 | 执行后跳转 `?schedule=` 并 `loadSchedules` |
| P1 占位 | 方案解释 / 审查 / 异常分析按钮（501 友好提示） |

---

## 联调结论（2026-06-23）

**总体：有条件通过**（F014 P0 主路径 API/UI 对齐通过；火山 ARK DeepSeek 解析与完整执行链通过；阶段 3/7 回归通过；**manager 调 `/ai/parse` 未返回 40300** 为 P1 偏差；无 P0 阻塞）

### 环境

| 项 | 值 |
|----|-----|
| 集成分支 | `integration/phase-8` @ `58560f0`（merge backend + frontend） |
| 后端 | `origin/backend/phase-8` @ `d49b3ce` |
| 前端 | `origin/frontend/phase-8` @ `81c9d75` |
| Mock | `AUTH/BASIC_DATA/SCHEDULE/NODE_DISPATCH/ROUTES/SIMULATION/EXCEPTIONS/AI` 均为 `false` |
| DeepSeek | 火山引擎 ARK：`DEEPSEEK_API_BASE=https://ark.cn-beijing.volces.com/api/v3`，`DEEPSEEK_MODEL=ep-20260623010143-nwscw`（Key 见本地 `.env`，勿提交） |
| 数据库 | 删 `src/backend/data/logistics.db` → `init_db()` → `init_demo_data.py` → `alembic stamp head` |
| 自测脚本 | [`test_phase8.py`](../src/backend/scripts/test_phase8.py)（dry-run）；完整用例见下文 API 实测 |
| 服务 | 后端 `localhost:8000`；前端 dev `localhost:5174`（5173 被占用时 Vite 自动切端口） |

### P0 结果

| 用例 | 结果 | 实测摘录 |
|------|------|----------|
| **T1 dry-run** | pass | `message` + `execute:false` → `code=0`，`mode=ai`，`executed=false`，`meta.degraded=false`（约 7.8s）；DeepSeek 返回 `distance:0.7` 等权重 |
| **T3 AI 新建调度** | pass | `GS20260623003`，`executed=true`，`mode=ai`，`degraded=false`（约 14.3s）；含 F007→F021→F005→F006（`demo_mode=true`） |
| **T2 AI 重规划** | pass | `GS20260623003` → `GS20260623004`，`replan_results` 1 条，`executed=true`（约 17.0s） |
| **T4 DeepSeek 降级** | pass | 清空 `DEEPSEEK_API_KEY` 后 dry-run → `code=0`，`meta.degraded=true`，`degraded_reason=DeepSeek API Key 未配置`，仍返回默认 `algorithm_params` |
| **T5 P1 占位** | pass | `POST /ai/explain` → `code=50100`，HTTP 200 |
| **T6 manager POST parse** | **P1 偏差** | `manager` 调 `POST /ai/parse`（dry-run）→ `code=0`（契约期望 `40300`）；后端 [`ai.py`](../src/backend/api/ai.py) 仅用 `get_current_user`，未 `require_dispatcher` |
| **阶段 3 传统全局调度** | pass | `POST /schedule/global` + `demo_mode:true` → `GS20260623005`（约 1.1s） |
| **阶段 7 redispatch 回归** | pass | node 异常 `EX1782160299884` + `action:redispatch` → `new_schedule_code=GS20260623006`，`version=2`，`is_replan=true`（约 1.7s） |
| **阶段 6 RBAC** | pass | manager `POST /schedule/global` → `40300` |
| 前端 `npm run build` | pass | ~1.1s |
| UI 页面可达 | pass | `http://localhost:5174/`、`/exceptions` → HTTP 200；Vite 代理 `/api/health` → `code=0` |

### 火山 ARK 配置说明

联调需配置三项（仅填 Key 不够，默认 `api.deepseek.com` 无法调用火山端点）：

```env
DEEPSEEK_API_KEY=<本地 .env>
DEEPSEEK_API_BASE=https://ark.cn-beijing.volces.com/api/v3
DEEPSEEK_MODEL=ep-20260623010143-nwscw
```

### UI 自测（API 对齐 + 页面可达）

Mock 全关、后端 8000 + 前端 dev 环境下：

| # | 页面 | 操作 | 结果 |
|---|------|------|------|
| 1 | Dashboard | 空库进入 | AI 面板可见（`81c9d75`：`v-if` 在方案列表外） |
| 2 | Dashboard | AI 发送并执行 | API `T3` pass；`useAiParse` 成功后 `router.push('?schedule=')` + `loadSchedules` |
| 3 | Dashboard | AI 重规划当前 | API `T2` pass；方案下拉应出现 v2 +「重规划」标签 |
| 4 | Dashboard | 仅预览参数 | API `T1` pass；结果区「参数来源」= `mode`，「已执行」= 否 |
| 5 | Dashboard | Key 无效时 | API `T4` pass；`lastMeta.degraded` → `el-alert` warning |
| 6 | Dashboard | P1 按钮 | API `50100`；`useAiParse.tryP1` 友好提示 |
| 7 | Dashboard | 传统「生成全局调度」 | API 阶段 3 pass |
| 8 | 异常管理 | node + redispatch | API 阶段 7 pass；与阶段 7 联调结论一致 |

### P1 建议（非阻塞）

| 项 | 说明 |
|----|------|
| **manager `/ai/parse`** | 契约写 `40300`，实测 `code=0`；前端 Dashboard 已 `v-if="isDispatcher"` 隐藏面板，答辩风险低；建议后端补 `require_dispatcher` 与契约对齐 |
| **前端 60s 超时** | `parseAi` timeout 60s；本次 AI 新建/重规划约 14～17s 未触顶；若数据量增大可提到 120s |
| **清库顺序** | 与阶段 7 相同：`init_db` + seed + `stamp head`；联调前**先停 uvicorn** 再删 `logistics.db` |
| **`test_phase8.py` T2** | 脚本硬编码 `GS20260623008`，空库会 `40401`；应用 `T3` 产出的 `schedule_code` 做重规划 |

### P2 遗留

| 项 | 说明 |
|----|------|
| P1 AI 能力 | `/ai/explain`、`/review`、`/analyze-exception` 占位 501，前端按钮已对接 |
| 路线产出时机 | 同阶段 7 P2；答辩主路径仍为 node + redispatch |

---

## Mock 自测路径（开发期）

1. `VITE_USE_MOCK_AI=true`（可与其他 Mock 同开）
2. 输入「优先缩短距离」→ 发送并执行 → 新 `GS…`
3. 选择方案 → 重规划当前 + 消息 → `replan_results` + v2
4. 仅预览参数 → `executed: false`，展示 `algorithm_params`
5. 消息含「降级测试」→ `meta.degraded` Alert

---

## 后续

- 前端 PR #22 → `main`
- 后端 `backend/phase-8` PR → `main`
- 可选：后端为 `/api/ai/parse` 补 dispatcher RBAC
