# 阶段 5 联调反馈（前端 → 后端）

> 本地 `integration/phase-5` 合并 `origin/backend/phase-5`（`294415e`）与 `origin/frontend/phase-5`（`18e6995`）后，五项 Mock 均为 `false` 的联调结论。

---

## 联调结论（2026-06-22）

**总体：通过**（阶段 3～4 回归 + 阶段 5 路径规划 API 与 Dashboard 路线可视化数据链路通过；前端已做响应结构适配）

### 环境

| 项 | 值 |
|----|-----|
| 集成分支 | `integration/phase-5` @ `86e3dca`（merge backend + 前端适配） |
| 后端 | `origin/backend/phase-5` @ `294415e` |
| 前端 | `frontend/phase-5` @ `18e6995` + 联调适配 commit |
| Mock | `AUTH/BASIC_DATA/SCHEDULE/NODE_DISPATCH/ROUTES` 均为 `false` |
| 数据库 | 重建 `logistics.db` + `init_demo_data.py`（50 订单 / 233 货物） |

### 已通过

| 项 | 结果 |
|----|------|
| `POST /api/schedule/global` | `code=0`，`schedule_code=GS20260622003` |
| `POST /api/schedule/node-dispatch`（`demo_mode=true`） | `code=0`，`BATCH20260622003`，`dispatches` 127 条 |
| `POST /api/routes/plan` | `code=0`，`routes` 127 条 |
| `GET /api/routes?batch_code=&vehicle_code=` | `code=0`，`total=1` |
| `GET /api/routes/{route_code}` | `route_segments` 非空，`total_time` 有值 |
| `GET /api/routes/by-vehicle/{code}/coordinates?batch_code=` | `code=0`（联调环境验证通过） |
| RBAC | manager `POST /api/routes/plan` → `40300` |
| 前端适配 | `route-normalize.ts` 对齐契约嵌套结构；真实模式走 `GET /routes` + `GET /routes/{code}` |
| `npm run build` | 通过 |

### 前端适配说明（非阻塞）

| 差异 | 处理 |
|------|------|
| 契约 `coordinates` 返回 `{ vehicle_code, routes[] }` 嵌套结构，架构 §6.5.4 为扁平 `nodes/packages/segments` | 新增 [`route-normalize.ts`](../src/frontend/src/utils/route-normalize.ts)；`nodes`/`packages` 从 `dispatch.tasks` 推导，`segments` 来自 `route_segments` |
| `coordinates` 接口无 `total_time`、无节点/包裹 | 通过 `GET /routes/{route_code}` 取 `total_time` 与路段 |
| 联调调用链 | 真实 API：`GET /routes?vehicle_code&batch_code` → `GET /routes/{route_code}`（规避路由顺序风险，见 P1） |
| F006 需手动触发 | 节点间调度后需 `POST /api/routes/plan`，否则路线列表为空 |

### P0 状态

无阻塞项（完成 `routes/plan` 后全链路可通）。

### P1 建议（可选）

| 项 | 说明 |
|----|------|
| F005 完成后自动触发 F006 | 契约 §7.2 写明自动规划，但 `dispatch_service.create_node_dispatch` 未调用 `RouteService`；前端 Dashboard 暂不要求用户手动 plan，联调时需先 plan |
| `GET /by-vehicle/.../coordinates` 路由注册顺序 | `/{route_code}` 注册在 `/by-vehicle/...` 之前，部分环境下可能将 `by-vehicle` 误匹配为 `route_code`；建议将 `/by-vehicle/{vehicle_code}/coordinates` 移到 `/{route_code}` 之前 |
| 架构 §6.5.4 与 `api-contract-phase5.md` 字段不一致 | 建议统一文档，或以契约为准更新架构说明书 |

### 后续

- 前端 `frontend/phase-5` 提 PR → `main`（含联调适配 + 本反馈文档）
- 后端 `backend/phase-5` 提 PR → `main`
- 可选：Dashboard 在节点间调度成功后自动调用 `POST /api/routes/plan`（dispatcher 角色）
