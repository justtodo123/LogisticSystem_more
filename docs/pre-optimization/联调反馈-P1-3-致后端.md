# 联调反馈 · P1-3 节点到货确认（致后端）

| 字段 | 值 |
| --- | --- |
| **阶段** | P1-3 / P1-08 |
| **前端分支** | `frontend/p1-3` @ `6154b21` |
| **后端分支** | `origin/backend/phase-p1-3` @ `d2f0163` |
| **集成分支** | `integration/p1-3` @ `7606103` |
| **文档日期** | 2026-06-09 |
| **联调状态** | Mock 自测 + 真实 API + **UI 手测通过**（integration/p1-3） |

---

## 1. 前端 API 适配（integration/p1-3）

| 项 | Mock/原契约 | 真实 API 适配 |
| --- | --- | --- |
| 批量确认 | `POST /confirm-arrival` + `items[].result` | `POST /confirm-arrival-batch` + `confirmations[].is_normal` |
| GET 列表 | `{ packages: [] }` | `data` 为数组，前端包装为 `GetArrivalPackagesResult` |
| 货物列 | 内嵌 `goods_items` | 后端未返回；联调层 `listPackages` 按 code 补全 |
| 结果摘要 | `normal_packages` / `cascade_*` | 从 `results[]` 映射；`new_package_code` → `activated_downstream_packages` |

改动文件：[`simulation.ts`](src/frontend/src/api/simulation.ts)、[`ArrivalConfirm.vue`](src/frontend/src/views/arrival/ArrivalConfirm.vue)

---

## 2. 联调环境

- `.env.local`：8 项 Mock 全 `false`
- DB：`python -m scripts.init_demo_data`（重建后执行）
- 登录：`dispatcher` / `123456`
- 后端：`uvicorn main:app --port 8000`
- 前端：`npm run dev`

**注意**：真实 `POST /simulation/deliver` 空 body 会送达库内全部 `in_transit` 包裹；Dashboard 联调请用**按车/按包裹**送达，勿点「全部送达」。

---

## 3. API 冒烟结果（`scripts/test_p1_3_integration.py`）

```
ALL_P1_3_API_CHECKS_PASS
```

| 场景 | 步骤 | 结果 |
| --- | --- | --- |
| 正常流 | preview→confirm→L0 dispatch→deliver(按包裹)→GET arrival→batch confirm(全 normal)→L1 dispatch | `triggered_repacking=true`，生成 `new_package_code` |
| C/D 异常语义 | 2 订单；C=normal、D=exception | D→`exception`；C 触发 repacking；L1 调度后仅 E' 类新包裹 `in_transit` |

示例（mixed flow）：

- `PKG202606250007` normal → `new_package_code=PKG17823765669450`
- `PKG202606250008` exception → `goods_status=exception`，`order_status=exception`

---

## 4. 手测清单（UI）

| 项 | 状态 |
| --- | --- |
| 侧栏进入 `/arrival-confirm` | 通过 |
| L1 节点下拉（L100x） | 通过 |
| 加载待确认包裹（deliver 后） | 通过 |
| C=normal、D=exception 提交 | 通过 |
| Dashboard L1→L2 后异常包裹不可 in_transit | 通过 |

---

## 5. 非阻塞建议（致后端）

1. `GET /simulation/arrival-packages` 响应补充 `goods_items`（减少前端二次查询）
2. 将 [`api-contract-p1-3.md`](docs/api-contract/api-contract-p1-3.md) v1.1 合入 main，与实现对齐
3. `POST /simulation/deliver` 可选支持 `schedule_code` / `batch_code` 过滤，避免误操作全局 in_transit

---

## 6. 结论

- **API 层**：P1-3 正常流 + 异常流冒烟通过
- **UI 层**：Dashboard + 到货确认页全流程手测通过
- **前端 PR**：`frontend/p1-3` → main（PR #34），已含联调 API 适配
- **后端依赖**：合 main 前需 `backend/phase-p1-3` 到货 API 就绪
