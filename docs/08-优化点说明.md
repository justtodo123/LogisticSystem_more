# 08 — 优化点说明

> 最后更新：2026-09-04 | 面向：第一轮 T0-T6 历史回顾与当前边界映射

> 本页保留第一轮 23 个 T0-T6 任务的历史摘要，不代表当前 R2 状态。R2-00～R2-06 工程交付已冻结；当前事实、证据和未验证边界以 [R2 收口记录](../My_doc/plan_todo/20260904-R2-closeout.md) 与 [Claim-to-evidence 台账](../My_doc/delivery/02-claim-evidence-ledger.md) 为准。

## 优化总览

项目原为 MVP 阶段完成、P1 功能交付的状态。通过 **23 个优化任务**（T0-1 ~ T6-2）在以下 7 个方面进行了全面加固。

---

## 阶段 0：地基 — 生产必备基础设施（5 任务）

| 任务 | 内容 | 价值 |
|------|------|------|
| **T0-1** 分环境配置 | dev/staging/prod 三环境 `.env.*`，pydantic-settings 集中管理，密钥入口加 `JWT_SECRET` | 消除硬编码密钥，支持 CI 多环境注入 |
| **T0-2** 细粒度 RBAC | 第一轮引入 `require_permission`/`require_role`；当前角色为 `admin`/`dispatcher`/`viewer`/`warehouse_operator` 与兼容 `manager` | 调度操作受后端权限依赖保护，前端只做 UI 提示 |
| **T0-3** 操作审计日志 | `middleware/audit_log.py` 记录操作事件 + IP + User-Agent | 可追溯操作历史 |
| **T0-4** 幂等控制 | `Idempotency-Key` 请求头去重（24h TTL），含输入校验增强 `core/validators.py` | 防止重复提交、网络重试副作用 |
| **T0-5** 服务降级与异常兜底 | 全局请求超时 30s + fallback 降级策略 + DeepSeek 超时 15s | 单点故障不拖垮整个服务 |

## 阶段 1：模型 — 业务模型完整性（4 任务）

| 任务 | 内容 | 价值 |
|------|------|------|
| **T1-1** 订单状态扩展 | 从 4 状态扩展到 6 状态（unassigned/assigned/in_transit/signed/exception/closed） | 覆盖真实物流全生命周期 |
| **T1-2** 运力模型增强 | Vehicle 新增 plate_number/time_window/route_limit/cost_per_km/load_rate_max；Driver 新增 shift/工时限制 | 支持时段约束、成本核算、排班管理 |
| **T1-3** 节点类型扩展 | 从 2 种扩展到 5 种（仓储/分拣/区域中心/分部/合作点） | 支持多级物流网络 |
| **T1-4** 状态机同步 | 更新 `ORDER_TRANSITIONS`，所有状态变更走单一入口 | 状态一致性保证 |

## 阶段 2：调度引擎 — 可插拔化 + 多目标优化（4 任务）

| 任务 | 内容 | 价值 |
|------|------|------|
| **T2-1** 策略模式重构 | `base.py` ABC + `factory.py`；当前全局/节点调度只注册 `greedy`/`dummy` | 算法可插拔；DeepSeek 是辅助层，不是调度 engine |
| **T2-2** 多目标评分引擎 | 5 目标归一化加权排序，Top-K 候选（≥3），响应含 score_breakdown/composite_score | 不只给出"一个方案"，还有备选和评分拆解 |
| **T2-3** 可解释性 | `algorithms/explainer.py` 生成评分拆解+约束命中+备选理由，落库 `explanation_data` 列 | 调度员理解"为什么选这个方案" |
| **T2-4** 人工干预 | 4 端点：换车/换司机/重算/撤销，含校验+版本链+前端 VehicleTaskTable 交互 | 调度员可纠正算法不可见的实际情况 |

## 阶段 3：异常 + 通知 — 闭环处理（2 任务）

| 任务 | 内容 | 价值 |
|------|------|------|
| **T3-1** 异常重规划增强 | partial/full/hybrid 三策略 + 差异报告 + 去重 | 精准重调度，影响范围可控 |
| **T3-2** 消息通知服务 | 可插拔渠道（Console/Email/企业微信），4 场景挂载（调度确认/异常/重规划/送达），运行时配置切换 | 关键事件主动通知，不依赖人工巡检 |

## 阶段 4：工程化 — 性能 + 可靠性（3 任务）

| 任务 | 内容 | 价值 |
|------|------|------|
| **T4-1** CI/CD | GitHub Actions ci.yml（pytest + vue-tsc build）+ cd.yml（GHCR 双镜像推送） | 每次提交自动验证，main 自动构建镜像 |
| **T4-2** 数据库索引 | 为高频查询补索引，并以 migration/查询专项测试验证结构与查询次数 | 改善查询路径；不保留缺少可复现场景的精确性能倍数 |
| **T4-3** Redis 缓存层 | `@cached` 装饰器 + 读写失效 + Redis 失败自动降级内存 | 列表热点缓存，写操作主动失效 |

## 阶段 5：外部对接 — 生态系统集成（3 任务）

| 任务 | 内容 | 价值 |
|------|------|------|
| **T5-1** ERP/WMS 导入导出 | CSV(xlsx) 导出（带 BOM）+ Excel 导入（列映射/skip_errors）+ ERP webhook（API Key/JWT 双鉴权） | 对接企业现有系统 |
| **T5-2** 地图服务集成 | 三档距离降级（路网 API → 直线×系数 → Haversine）+ 高德地图真实路径渲染 + 无 Key 降级 Canvas 折线 | 开发/演示无阻塞，生产有 Key 自动升级精度 |
| **T5-3** 报表分析模块 | 4 类报表（SLA/成本/异常/运力）+ Dashboard 前端（KPI 卡片 + 图表） | 管理层可视化决策 |

## 阶段 6：AI 加固 — 可靠性 + 可控性（2 任务）

| 任务 | 内容 | 价值 |
|------|------|------|
| **T6-1** AI 输出结构化校验 | Pydantic Schema 校验 + 错误反馈重试（最多 3 次）+ 业务规则闸门（权重和≈1） | AI 输出不可控→可控，错误不静默通过 |
| **T6-2** AI 建议确认闸门 | `AiSuggestion` 模型（info/suggestion/action 三级）+ 确认/拒绝端点 + 前端按钮 | AI 只建议不执行，人工确认后生效 |

---

## 历史测试快照

下表只保留各阶段当时的记录，不作为当前测试总数；命令、日期、commit 和适用范围应从 [Claim-to-evidence 台账](../My_doc/delivery/02-claim-evidence-ledger.md) 核验。

| 时间点 | 测试用例数 | 说明 |
|--------|-----------|------|
| MVP 交付后 | ~423 | 历史 |
| T0-T6 优化后（2026-08-10） | 626 | 历史里程碑，当时全绿 |
| 2026-08-19 00A 既有基线 | 635 passed, 196 warnings | 校准前引用 |
| 2026-08-19 01 全量 | 645 passed, 196 warnings | `src/backend` `python -m pytest -q -p no:cacheprovider` |

---

## 第一轮边界与当前 R2 映射

### 仍开放

| 条目 | 状态 | 说明 | 跟踪 |
|------|------|------|------|
| L1 容量检查维度 | 仍存在 | 读取 DB 全局 `packed` 包裹量；`max_storage_time` 以体积×小时比较 | 算法边界，未单独立项 |
| 2-opt 实现 | stub | `route_planning.py` 不执行实际路径交换 | 算法边界 |
| 评分 load_rate | 仍存在 | 跨候选恒定 0.5（分母用车辆包裹容量） | 算法边界 |
| `algorithm=deepseek` | mitigated | 工厂仅注册 greedy/dummy | [proced 008](../proced_problem/008-deepseek-algorithm-not-implemented.md)，不得标 done |
| Docker / 生产业务 E2E | 未验证 | 第一轮 02A 仅本机进程 smoke；R2 已有远端 workflow 与镜像发布证据，但本机 Compose、02B、预发/生产仍未验收 | [R2 收口记录](../My_doc/plan_todo/20260904-R2-closeout.md) |

### 已修复（不再作为当前遗留）

| 条目 | 证据 |
|------|------|
| `time_window` 未校验 | 2026-08-19 方案 A：自由文本时效要求，strip/非空/≤32；见 [plan 04](../My_doc/post_plan/第一轮优化计划/04-time-window-contract-decision.md) |
| 路线分页与 N+1 | 2026-08-19 `GET /api/routes` 返回 items/total/page/page_size，join 查询次数恒定；见 [plan 05](../My_doc/post_plan/第一轮优化计划/05-route-pagination-and-query-efficiency.md) |
| 订单前后端状态漂移 | 2026-08-19 起前后端/Mock 共用六态；未知值不静默映射；见 [plan 03](../My_doc/post_plan/第一轮优化计划/03-order-status-contract.md) |
| 审计中间件无用户 | `0e9e089`：`get_current_user` 写入 `request.state.current_user` |
| `arrival_confirm` 无鉴权 | `846ba3f`：三端点 `require_dispatcher` |
| AI 建议查 `pending` | `2e0c6e8`：改为 `unassigned` |
| ERP webhook JWT 无角色 | `846ba3f`：回退 JWT 要求 dispatcher/admin |
| delivered 重规划回退 | 2026-08-19：重置不再包含 delivered；F007 跳过终态货物。[proced 010](../proced_problem/010-delivered-goods-reset-on-replan.md) |

`blocked` / `needs_decision` / `mitigated` 不计为 `done`。
