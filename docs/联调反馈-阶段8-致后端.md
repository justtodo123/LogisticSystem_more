# 阶段 8 联调反馈（前端 → 后端）

> 前端 `frontend/phase-8` 实现 F014 AI 助手 UI；**完整联调需在 merge `origin/backend/phase-8` 后进行**。

---

## 前端交付摘要

| 项 | 说明 |
|----|------|
| 分支 | `frontend/phase-8` |
| 页面 | Dashboard 内嵌 `AiAssistantPanel`（dispatcher 可见） |
| API | [`ai.ts`](../src/frontend/src/api/ai.ts)（`parseAi` 60s timeout + `postWithMeta` 保留降级信息） |
| Mock | `VITE_USE_MOCK_AI`；联调时 **8 项 Mock 均为 `false`** |
| 契约 | 对齐 `origin/backend/phase-8:docs/api-contract/api-contract-phase8.md` |
| 构建 | `npm run build` 通过 |

---

## 功能清单

| 能力 | 说明 |
|------|------|
| 自然语言 | textarea + 发送并执行 / 仅预览参数（dry-run） |
| 执行目标 | 新建调度 / 重规划当前方案 / 多选历史方案 |
| 手动权重 | F007/F005 权重滑块 + F006 max_iterations |
| 降级展示 | `meta.degraded=true` → `el-alert` warning，不伪装 AI 成功 |
| 成功刷新 | 执行后跳转 `?schedule=` 并 `loadSchedules` |
| P1 占位 | 方案解释 / 审查 / 异常分析按钮（501 友好提示） |

---

## Mock 自测路径

1. `VITE_USE_MOCK_AI=true`（可与其他 Mock 同开）
2. 输入「优先缩短距离」→ 发送并执行 → 新 `GS…`
3. 选择方案 → 重规划当前 + 消息 → `replan_results` + v2
4. 仅预览参数 → `executed: false`，展示 `algorithm_params`
5. 消息含「降级测试」→ `meta.degraded` Alert

---

## 联调前置

| 项 | 要求 |
|----|------|
| 集成分支 | `integration/phase-8` = merge `backend/phase-8` + `frontend/phase-8` |
| Mock | 全部 8 项 `VITE_USE_MOCK_*` = `false` |
| 后端 Key | `DEEPSEEK_API_KEY`（无 Key 时仍应 `code=0` + `meta.degraded=true`） |
| 账号 | `dispatcher / 123456` |

---

## 联调结论

**状态：待联调**

### P0 用例（待填）

| 用例 | 预期 |
|------|------|
| AI 新建调度 | `schedule_code` + `executed=true` |
| AI 重规划 | `replan_results` + Dashboard v2 |
| dry-run | `executed=false`，库无新方案 |
| DeepSeek 降级 | `meta.degraded=true` + Alert |
| manager POST parse | `40300` |
| 阶段 3～7 回归 | 传统「生成全局调度」仍可用 |

---

## 后续

- 前端 PR → `main`
- 后端 `backend/phase-8` PR → `main`
- `integration/phase-8` 联调后更新本文件「联调结论」
