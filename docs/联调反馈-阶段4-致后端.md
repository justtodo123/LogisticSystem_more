# 阶段 4 联调反馈（前端 → 后端）

> 本地 `integration/phase-4` 合并 `origin/backend/phase-4`（`07e1231`）与 `frontend/phase-4`（`f5d1017`）后，四项 Mock 均为 `false` 的联调结论。

---

## 联调结论（2026-06-22）

**总体：通过**（阶段 3 回归 + 阶段 4 核心 API 与 Dashboard 数据链路通过；前端已做响应结构适配）

### 环境

| 项 | 值 |
|----|-----|
| 集成分支 | `integration/phase-4` @ `fdc892b`（merge backend + 前端适配） |
| 后端 | `origin/backend/phase-4` @ `07e1231` |
| 前端 | `frontend/phase-4` @ `f5d1017` |
| Mock | `AUTH/BASIC_DATA/SCHEDULE/NODE_DISPATCH` 均为 `false` |
| 数据库 | 重建 `logistics.db` + `init_demo_data.py`（50 订单 / 248 货物） |

### 已通过

| 项 | 结果 |
|----|------|
| 阶段 3 回归 `POST /api/schedule/global` | `code=0`，`package_count=78` |
| `GET /api/schedule/global/{code}` | 含 `goods_schedules` |
| `POST /api/schedule/node-dispatch`（`demo_mode=true`） | `code=0`，返回 `BATCH20260622001`，`dispatches` 非空 |
| `GET /api/schedule/batches?schedule_code=` | 含 `l0_l1_dispatch_count`、`l1_l2_dispatch_count` |
| `GET /api/schedule/batches/{batch_code}` | 含扁平 `dispatches[]`（`level_phase`、`tasks`、`total_distance`） |
| RBAC | manager `POST node-dispatch` → `40300` |
| 无效 `schedule_code` | `40401` |
| 前端适配 | `dispatch-normalize.ts` 对齐契约扁平结构；`VehicleTaskTable` 按 `level_phase` 分组展示 |
| `npm run build` | 通过 |

### 前端适配说明（非阻塞）

| 差异 | 处理 |
|------|------|
| 契约 `dispatches` 为**扁平**列表（每车一条），Mock 原为按阶段分组 | 新增 [`dispatch-normalize.ts`](../src/frontend/src/utils/dispatch-normalize.ts) 统一扁平化 |
| 列表项无 `vehicle_count` | 前端用 `l0_l1_dispatch_count + l1_l2_dispatch_count` 展示 |
| `POST node-dispatch` 响应无 `l0_l1_dispatch_count` 字段 | 前端从 `dispatches[].level_phase` 计数（POST 响应内 `level_phase` 可能缺失，以 GET 详情为准） |

### P0 状态

无阻塞项。

### P1 建议（可选）

| 项 | 说明 |
|----|------|
| POST 响应补充 `l0_l1_dispatch_count` / `l1_l2_dispatch_count` | 与列表/契约示例一致，减少前端推断 |
| POST 响应 `dispatches[]` 补充 `level_phase` | 便于创建后立即展示摘要 |

### 后续

- 前端 `frontend/phase-4` 提 PR → `main`
- 后端 `backend/phase-4` 提 PR → `main`
