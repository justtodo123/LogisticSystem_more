# 阶段 3 联调反馈（前端 → 后端）

> 本地 `integration/phase-3` 合并 `origin/backend/phase-3`（`5e89fbb` 附近）后，`VITE_USE_MOCK_SCHEDULE=false` 第一轮联调结论。

---

## 第一轮联调结论（2026-06-14）

**总体：通过**（修复 seed 脚本后 API 14/14 项通过；前端已做小修）

### 已通过

| 项 | 结果 |
|----|------|
| POST /api/schedule/global | `code=0`，<10s，返回完整摘要 |
| GET /api/schedule/global | 列表含 `created_at` |
| GET /api/schedule/global/{code} | 含 `goods_schedules`、`packages` |
| POST 摘要 vs GET 详情 | `total_goods`、`score` 一致 |
| path 格式 | `SC* → L1* → L2*` |
| 副作用 | pending→delivering；packages 非空（含 F021）；goods→packed |
| 无 pending 再 POST | `code=40001` |
| RBAC | manager POST→403；GET 可读 |
| 无效 schedule_code | `code=40401` |
| 前端联调 | 关 Mock 后 POST→列表→详情链路正常；`npm run build` 通过 |

### 需修复（P0）

**`init_demo_data.py` 引用未定义变量 `l0_nodes`**

- 文件：`src/backend/scripts/init_demo_data.py` `_create_orders_and_goods`
- 现象：`python scripts/init_demo_data.py` 在创建订单阶段 `NameError: l0_nodes`
- 影响：无法初始化 pending 订单，阻塞联调
- 建议：改为 `sc_nodes = db.query(Node).filter(node_type=="storage_center")`，货物起点用存储中心

（联调时前端本地已临时修复，请合入 `backend/phase-3`）

### 前端已做小修（已合入 `frontend/phase-3`）

| 问题 | 处理 |
|------|------|
| `total_time` 契约单位为**小时**，UI 误标「分钟」 | 改为「总时间 (小时)」 |
| POST 传 `order_codes: []` | 改为省略字段，与契约「不传=全部 pending」一致 |

---

## 环境备忘

```powershell
# 重建库前需停 uvicorn，否则 logistics.db 被占用
cd src\backend
.\.venv\Scripts\Activate.ps1
$env:PYTHONPATH="."
python scripts/init_demo_data.py
uvicorn main:app --reload --port 8000
```

```env
# src/frontend/.env.local 联调
VITE_USE_MOCK_AUTH=false
VITE_USE_MOCK_BASIC_DATA=false
VITE_USE_MOCK_SCHEDULE=false
```

---

## 第二轮联调结论（2026-06-14）

**总体：通过**（后端 `8483ea4` 修复后，无需本地 seed 补丁；API 14/14 + 前端链路通过）

### 环境

| 项 | 值 |
|----|-----|
| 集成分支 | `integration/phase-3` @ `4446731`（从 `frontend/phase-3` 重建并 merge） |
| 后端 | `origin/backend/phase-3` @ `8483ea4` |
| 前端 | `frontend/phase-3` @ `8ab652f` |
| Mock | 三项均为 `false` |

### 已通过

| 项 | 结果 |
|----|------|
| `init_demo_data.py`（P0） | 无需本地修改；50 订单 / 238 货物初始化成功 |
| `schedule_service` 状态流转 | 订单 `pending→delivering`；货物/包裹 `packed`；55 packages |
| POST /api/schedule/global | `code=0`，约 1.25s |
| GET 列表 / GET 详情 | 含 `created_at`、`goods_schedules`、`packages` |
| POST 摘要 vs GET 详情 | `total_goods`、`score` 一致 |
| path 格式 | `['SC*', 'L1*', 'L2*']`（如 `SC004 → L1001 → L2024`） |
| 40001 / 40401 / RBAC | 均符合契约 |
| 前端 Vite 代理链路 | 登录 → POST 生成 → GET 列表 → GET 详情 通过 |
| `npm run build` | 通过 |

### P0 状态

| 问题 | 状态 |
|------|------|
| `l0_nodes` 未定义 | **已修复**（`8483ea4` 中正确定义为存储中心查询） |

### 后续

- 前端可 `git push origin frontend/phase-3` 并提 PR → `main`
- 后端 `backend/phase-3`（含 `8483ea4`）可提 PR → `main`
