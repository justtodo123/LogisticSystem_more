# DeepSeek 相关 - 用于 Python 开发（更新与 docs/环境配置说明.md 保持一致）
# 直接 pip install -r requirements.txt

# ---------- Web 框架 ----------
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# ---------- 数据库 ----------
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg[binary]>=3.2.0

# ---------- 配置与校验 ----------
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ---------- 认证与加密 ----------
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4

# ---------- HTTP 客户端（DeepSeek 调用） ----------
httpx>=0.27.0

# ---------- Excel 读写 ----------
openpyxl>=3.1.0
pandas>=2.0.0

# ---------- 数据计算 ----------
numpy>=1.26.0

# ---------- 测试 ----------
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-md>=0.2.0
httpx>=0.27.0  # TestClient 支持
coverage>=7.14.0
pytest-cov>=7.1.0

# ---------- Redis 缓存（T4-3） ----------
redis>=5.0.0
