# DeepSeek 路径优化 - 智能物流平台 · Python 依赖
# 路径：src/backend/requirements.txt
# 新增包时请同步更新本文件，见 docs/开发规范.md
# 安装：pip install -r requirements.txt

# ---------- Web 框架 ----------
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# ---------- 数据库 ----------
sqlalchemy>=2.0.0
alembic>=1.13.0

# ---------- 数据校验与配置 ----------
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ---------- 认证 ----------
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4

# ---------- HTTP 客户端（DeepSeek 等） ----------
httpx>=0.27.0

# ---------- 数据处理（订单导入、演示数据） ----------
openpyxl>=3.1.0
pandas>=2.0.0

# ---------- 数值计算（调度算法） ----------
numpy>=1.26.0

# ---------- 测试 ----------
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0  # TestClient 依赖
