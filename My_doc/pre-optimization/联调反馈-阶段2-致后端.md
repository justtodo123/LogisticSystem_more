# 阶段 2 联调反馈（前端 → 后端）

> 复制下方「消息正文」发给同学 A 即可；细节见后文附录。

---

## 消息正文（可直接复制）

同学 A 你好，

我这边 `frontend/phase-2` 已和 `backend/phase-2` 在本地联调，整体列表和大部分写操作 OK，感谢！下面有几处阻塞/规范问题需要你那边修一下，修完我这边可以再跑一轮联调。

**环境**
- 分支：`backend/phase-2`（commit: `ddd75b1` 附近）
- 启动：`PYTHONPATH=. python scripts/init_demo_data.py` 后 `uvicorn main:app --port 8000`
- 前端：`VITE_USE_MOCK_BASIC_DATA=false`，真实调 API

**已通过**
- 登录 / `GET /api/orders|nodes|vehicles|goods|drivers|packages`
- seed 数据量正常（订单约 50、货物约 241、车辆/司机各 70 等）
- `POST /api/orders`（含 `goods[]`）、`PUT` pending 订单、节点/车辆 CRUD
- 分页格式 `{ items, total, page, page_size }` 与契约一致

**需修复（按优先级）**

1. **【P0】写接口未挂 RBAC / 认证**  
   `require_dispatcher`、`get_current_user` 目前只在 `auth.py` 使用；orders/nodes/vehicles 等 POST/PUT/DELETE 未鉴权。manager 用 Postman 仍可写，不符合 MVP「管理者只读」。  
   → 请所有写接口挂 `Depends(require_dispatcher)`，读接口至少 `Depends(get_current_user)`。

2. **【P0】删除 pending 订单 500**  
   删除有货物的订单时报错：`NOT NULL constraint failed: goods.order_id`（先 UPDATE goods 把 order_id 置空导致）。  
   → 删除订单前应先删关联 goods，或配置 ORM 级联删除。

3. **【P1】`GET /api/nodes` 列表缺扩展字段**  
   列表未返回 `level`、`capacity`、`inventory`、`max_storage_time` 等；也无法按 `level` 筛选。  
   订单「目的地」需 0 级分拣中心，我前端临时用 `L2*`/`L1*` 编号规则过滤，不稳定。  
   → 列表项补全扩展字段，或增加 `?level=0` 查询参数。

4. **【P1】创建订单未校验目的地类型**  
   目前只校验 node 存在，未限制为 `sorting_center` 且 `level=0`（PRD F001）。  
   → 创建时校验目的地必须是 0 级分拣中心。

5. **【P2】`GET /api/goods` 无 `order_code` 筛选**  
   前端货物页有按订单号筛选，参数会被忽略（非阻塞，有空再加）。

**说明（非 bug）**
- 包裹列表为空：seed 未生成 packages，阶段 3 调度后才有，前端已接受空列表。

修好后麻烦同步一下 Swagger / `api-contract-phase2.md` 是否有变更，我关 Mock 再测一轮。谢谢！

---

## 附录 A：复现步骤

### 删除订单 500

```http
POST /api/auth/login
{ "username": "dispatcher", "password": "123456" }

POST /api/orders
Authorization: Bearer {token}
{
  "destination_node_code": "L2001",
  "time_window": "10:00-12:00",
  "goods": [{ "goods_name": "测试", "goods_type": "测试", "weight": 1, "volume": 0.01 }]
}

DELETE /api/orders/{上一步返回的 order_code}
```

预期：pending 订单可删；实际：`code: 50000`，SQLite 外键/NOT NULL 错误。

### RBAC 未生效

用 `manager / 123456` 登录拿 token，直接：

```http
POST /api/nodes/storage-centers
Authorization: Bearer {manager_token}
{ ... }
```

预期：403；实际：可能 200 成功。

### 节点列表无 level

```http
GET /api/nodes?node_type=sorting_center&page_size=5
```

响应 `items[]` 中无 `level` 字段，无法区分 0 级/1 级分拣。

---

## 附录 B：前端已做兼容（后端修后可简化）

| 问题 | 前端临时处理 | 文件 |
|------|-------------|------|
| 导入响应字段名 | `failed_count` → `fail_count` | `src/frontend/src/api/orders.ts` |
| 订单创建需 goods | 新增订单表单含货物明细 | `src/frontend/src/views/orders/OrderList.vue` |
| 0 级分拣下拉 | `L2*`/`L1*` 编号启发式 | `src/frontend/src/api/nodes.ts` |

---

## 附录 C：相关文档

- API 契约：`docs/api-contract/api-contract-phase2.md`
- 后端 README 阶段 2：`src/backend/README.md`
- 集成分支（本地联调用）：`integration/phase-2`
