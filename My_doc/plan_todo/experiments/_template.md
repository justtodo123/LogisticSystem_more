# 实验记录模板

复制本文件为 `YYYYMMDD-<plan-id>-<slug>.md`，例如 `20260825-R2-01-confirm-cas.md`。
结论必须绑定代码、环境、命令和产物；禁止只写“已通过”。不存在的 commit、PR 或运行结果写“尚无/未执行”，不得预填。

## 元数据

- 计划 ID：
- 计划/决策版本：
- 日期与时区：
- 执行人：
- 层级：P0 本机协议 / P1 外部拓扑
- Git 分支：
- Commit SHA：
- PR URL：
- CI run URL（如有）：

## Schema 与数据来源

- Alembic 当前 revision：
- Alembic heads：
- 数据库来源：fresh / Alembic-managed legacy / 无 alembic_version 的混合旧库
- 升级前 revision / schema 指纹：
- 数据规模与种子方式：
- 数据是否为合成/脱敏数据：

## 环境

- OS / CPU / 内存 / 磁盘：
- Python / Node：
- 数据库（引擎、版本、隔离级别、连接池）：
- Redis（有/无、版本）：
- 应用 worker / 后台 worker 数：
- 关键依赖或容器镜像版本：

## 场景

- 目标与不变量：
- 请求分布 / 并发客户端：
- 预热 / 持续时间：
- 故障注入点（若有）：
- 对照组 / 基线（若有）：

## 命令

```text
# 粘贴完整、已脱敏的命令
```

## 原始结果与产物

- 命令是否实际执行：是 / 否
- 退出码：
- 摘要（通过数 / 失败数 / 关键日志）：
- 追踪内摘要路径：
- 外部原始产物位置 / CI artifact URL：
- 产物大小：
- SHA-256：
- 保留期限 / 删除日期：
- 脱敏检查：已检查 / 未检查；检查人及说明：
- 访问限制或复现障碍：

## 结论

- 状态：通过 / 失败 / blocked / 未执行
- 结论与对应证据：
- 已知限制（尤其 SQLite 写锁、单 worker、无 Redis）：
- 未通过项 / 未执行项：
- 下一步：
