# 20260904 R2-06 镜像发布扫描门禁

## 元数据

- 计划 ID：R2-06
- 计划/决策版本：第二轮 README 2026-09-04；策略 `2026-09-03.1`；观测裁剪 [D-R2-OBS](../decisions.md)
- 日期与时区：2026-09-04 Asia/Shanghai
- 执行人：Codex
- 层级：P2 镜像发布扫描门禁（CD 发布门禁，不是定时 cron，也不是本机 Docker 部署）
- Git 分支：main（实现分支 `fix/image-base-security-refresh`）
- Commit SHA：f9e08a499ba50987505e32d58b545a37c9543ef4
- PR URL：https://github.com/justtodo123/LogisticSystem_more/pull/40
- CI run URL：https://github.com/justtodo123/LogisticSystem_more/actions/runs/33826209581
- CD run URL：https://github.com/justtodo123/LogisticSystem_more/actions/runs/33826520856

## Schema 与数据来源

- Alembic 当前 revision：本卡无 schema 变更
- Alembic heads：不适用
- 数据库来源：不适用（镜像构建/扫描，未启动业务库）
- 升级前 revision / schema 指纹：不适用
- 数据规模与种子方式：不适用
- 数据是否为合成/脱敏数据：是（仅扫描摘要；无业务数据）

## 环境

- OS / CPU / 内存 / 磁盘：GitHub Actions ubuntu-latest；扫描平台 Linux-6.17.0-1022-azure-x86_64-with-glibc2.39
- Python / Node：backend 运行层为 debian:forky-slim 的 `python3` venv（扫描路径显示 Python 3.14）；frontend 构建阶段 `node:22-alpine`，运行阶段 `nginx:1.30.4-alpine3.24`
- 数据库：无
- Redis：无
- 应用 worker / 后台 worker 数：无（未拉起服务）
- 关键依赖或容器镜像版本：
  - backend 基础层 `debian:forky-slim`（扫描 OS：debian forky/sid）
  - frontend 运行层 `nginx:1.30.4-alpine3.24`（扫描 OS：Alpine 3.24.1）
  - Trivy 0.56.2；Docker 28.0.4
  - 策略 `security/image-scan-policy.json` version `2026-09-03.1`
  - exceptions `security/image-scan-exceptions.json`：`exceptions: []`

## 场景

- 目标与不变量：CRITICAL/HIGH（及策略下的 UNKNOWN）不得阻断发布；未登记 exception；门禁通过后才推 GHCR SHA 与 latest
- 请求分布 / 并发客户端：不适用
- 预热 / 持续时间：CD job 约 01:38:17Z–01:42:34Z
- 故障注入点：无
- 对照组 / 基线：历史阻断 CD [run 33765319991](https://github.com/justtodo123/LogisticSystem_more/actions/runs/33765319991)（`python:3.13-slim` debian 13.6 + `nginx:1.27-alpine` Alpine 3.21.3）

## 命令

```text
# 实现已合入 main；本记录提取已成功的 CD 扫描产物，不在本机重复构建。
gh run view 33826520856 --json conclusion,headSha,url
gh run download 33826520856 -n image-scan-f9e08a499ba50987505e32d58b545a37c9543ef4
```

## 原始结果与产物

- 命令是否实际执行：是（远端 CD；本地只下载摘要）
- 退出码：CD conclusion=success；scan-status 全 0
- 摘要：
  - policy-evaluation `passed=true`，`exceptions_applied=[]`
  - backend `passed=true`，`blocked=[]`，`counts={MEDIUM: 1}`
  - frontend `passed=true`，`blocked=[]`，`counts={}`
  - 唯一残留 CVE-2026-13346：pip 26.1.2，fixed in 26.2.0，MEDIUM，status=fixed，属于 report-only，不阻断
- 追踪内摘要路径：`My_doc/plan_todo/experiments/r2-06-image-scan/`
  - `policy-evaluation.json` 511 B SHA-256 `b18b0ea5b11439e5611593022e31149367f4c9b8dc0b00367e44e30fa28305e0`
  - `env-report.json` 1482 B SHA-256 `5e358b89f7d0786a8e4db24025144544a801c9c4a467cc1acd23072171951aef`
  - `scan-status.json` 72 B SHA-256 `2be61cd2ff909dc059fbde75adb5db49da19aca3abc39332e3f4d3d9e0adf427`
- 外部原始产物位置 / CI artifact URL：https://github.com/justtodo123/LogisticSystem_more/actions/runs/33826520856 （artifact `image-scan-f9e08a499ba50987505e32d58b545a37c9543ef4`）
- 产物大小：见 env-report.json（backend-trivy 11550 B，frontend-trivy 16431 B，backend SBOM 261169 B，frontend SBOM 110663 B）
- SHA-256：见 env-report.json
- 保留期限 / 删除日期：CI artifact 14 天
- 脱敏检查：已检查；入库仅为 policy/env/scan-status 摘要，不含 Trivy 明细、SBOM、凭据
- 访问限制或复现障碍：原始 Trivy/SBOM 不入库；artifact 过期后需重新跑 CD 才能复现原始 JSON

## 结论

- 状态：通过（零 exception 发布）
- 结论与对应证据：切换到 `debian:forky-slim` 与 `nginx:1.30.4-alpine3.24` 后，CD 扫描门禁通过并推送 GHCR。未登记任何 exception。
- 已知限制：
  - 这是发布门禁，不是定时扫描，也不是生产部署验证
  - 本机无 Docker；未执行 compose / E2E / 线上拉起
  - 第一轮 02B 仍为 `mitigated`
  - backend 仍有 1 个 MEDIUM pip CVE，不阻断
  - Dockerfile.backend 注释仍写安装 Python 3.13，forky 默认 `python3` 扫描路径为 3.14；不把注释差异写成门禁失败
- 未通过项 / 未执行项：Grafana / Prom / OTel、跨 worker 指标聚合、定时镜像扫描、更长 soak/泄漏断言、完整写+读容量、本机/生产部署验证
- 下一步：文档收口合入 `main`。生产部署验证与剩余 P2 增强另列，不阻塞 R2 工程交付完成。
